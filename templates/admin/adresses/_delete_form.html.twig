<form method="post" action="{{ path('adresses_delete', {'id': adress.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adress.id) }}">
    <button class=" w-full self-center bg-red-500 hover:bg-red-700 text-white py-1 rounded ">Delete <i class="fa fa-trash" aria-hidden="true"></i></button>
</form>