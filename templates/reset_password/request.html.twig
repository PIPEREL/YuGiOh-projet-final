{% extends 'base.html.twig' %} {% block title %}mot de passe oublié{% endblock %} {% block body %} {% for flashError in app.flashes('reset_password_error') %}
<div class="alert alert-danger" role="alert">{{ flashError }}</div>
{% endfor %}
<h2 class="pt-4 ml-4 text-4xl font-bold">Mot de passe oublié</h2>
<hr class="my-5">
<div class="flex justify-center">
    <hr class="my-5"> {{ form_start(requestForm,{'attr':{'class': 'flex flex-col w-1/2 content-center gap-6 pb-10'}})}}{{ form_widget(requestForm.email,{'attr':{'class': 'py-2 px-3 rounded-lg border-2 border-purple-300 mt-1 focus:outline-none focus:ring-2 focus:ring-purple-600
    focus:border-transparent'}})}}


    <button class="self-center w-1/3 bg-purple-500 hover:bg-purple-700 text-white py-1 mt-5 rounded">envoyer</button> {{ form_end(requestForm) }}
</div> {% endblock %}